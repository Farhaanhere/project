import 'package:flutter/material.dart';

class screenaccount extends StatelessWidget {
  const screenaccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Text('Screen Acount'),
    );
  }
}
