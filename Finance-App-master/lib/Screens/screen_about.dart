import 'package:flutter/material.dart';

class screenabout extends StatelessWidget {
  const screenabout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Text('Screen About'),
    );
  }
}
